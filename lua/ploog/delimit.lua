local opts = {
  expand_space = 1,
  smart_quotes = 1,
  expand_inside_quotes = 1,
  jump_expansion = 1,
  balance_matchpairs = 1,
  expand_cr = 2
}

VG("delimitMate_", opts)

-- vg.loaded_delimitMate = 1 -- turn off plug
